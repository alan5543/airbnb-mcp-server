# smithery.yaml
version: 1
build:
  # Command to install dependencies
  install: pip install -r requirements.txt
  # Command to start your server
  # We use the python file directly. Smithery handles the rest.
  start: python main.py